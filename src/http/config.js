// 配置
const host = 'http://testapi.awbchina.com' // 生产
const timeout = 20000;
const appid = '';
const appKey = '';

const config = {
    host,
    timeout,
    appid,
    appKey,
}
export default config;